<div class="center jumbotron">
  <h1>Compare Two Images </h1>
  <p>Press run to setup the caffe net with specified two image paths and ending layer</p>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%= form_for(@compare, url: main_compare_result_path) do |f| %>
          <%= f.label "First Image" %>
          <%= f.text_field :first %>

          <%= f.label "Second Image" %>
          <%= f.text_field :second %>

          <br>
          <br>
          <%= f.label :layer %>
          <%= f.text_field :layer %>

          <%= f.label 'Top-k' %>
          <%= f.text_field :topk %>
          <br>
          <br>
          <%= f.submit "Run now!", class: "btn btn-lg btn-primary" %>
      <% end %>
    </div>
  </div>
</div>

